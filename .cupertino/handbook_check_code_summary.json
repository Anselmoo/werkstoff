{
  "domain": "code",
  "generatedAt": "2026-09-07T06:07:11Z",
  "findings": [
    {
      "file": "plugins/self-assess/hooks/guard_target_edit.py",
      "line": 125,
      "severity": "Medium",
      "title": "Guard fails open (allow) on internal ImportError instead of denying",
      "evidence": "try:\n    from lib.settings import load_settings  # noqa: E402\n    ...\nexcept (ImportError, ModuleNotFoundError) as exc:\n    print(f\"guard_target_edit: internal error ({type(exc).__name__}: {exc}); ... Allowing this edit rather than denying every future edit in this repo ...\", file=sys.stderr)\n    return allow()",
      "mechanical": false,
      "suggestedFix": "This is inside the opted-in branch (a self-assess edit-scope lock is already confirmed open at this point), so an unexpected import/packaging failure here should deny (return deny(...)) with the exception named and the documented ESCAPE_HATCH, not silently allow the edit through. If the intentional fail-open-on-packaging-defect tradeoff (from issue #24) is to be kept, it needs an explicit, named escape hatch of its own and should be called out as a deliberate rule exception rather than silently contradicting the fail-closed contract.",
      "rule": "Every PreToolUse/hook guard script must wrap its top-level logic in a broad try/except that fails closed on any unexpected exception (deny the action, exit non-zero) rather than failing open or crashing silently, and the denial message must both name the internal error and point to a specific, named escape-hatch mechanism (e.g. an env var or config flag) for overriding it.",
      "dimension": "error-handling"
    },
    {
      "file": "plugins/confab/scripts/hooks/guard_edit_scope.py",
      "line": 96,
      "severity": "Medium",
      "title": "Guard fails open (allow) on internal ImportError instead of denying",
      "evidence": "try:\n    from lib.paths import UnsafeWritePathError, safe_repo_path  # noqa: E402\n    from lib.remediation_scope import read_scope, mark_consumed  # noqa: E402\nexcept (ImportError, ModuleNotFoundError) as exc:\n    print(f\"guard_edit_scope: internal error ({type(exc).__name__}: {exc}); ... Allowing this edit rather than denying every future edit in this repo ...\", file=sys.stderr)\n    return allow()",
      "mechanical": false,
      "suggestedFix": "This check runs only after confirming analysis/confab/ exists (i.e. the repo has opted into confab), so an unexpected import failure at this point should deny with the exception named and the documented ESCAPE_HATCH rather than allow. Keep the fail-open behavior only if it is intentionally scoped and documented as a named, separate exception to the fail-closed contract, not folded silently into the general 'internal error' path.",
      "rule": "Every PreToolUse/hook guard script must wrap its top-level logic in a broad try/except that fails closed on any unexpected exception (deny the action, exit non-zero) rather than failing open or crashing silently, and the denial message must both name the internal error and point to a specific, named escape-hatch mechanism (e.g. an env var or config flag) for overriding it.",
      "dimension": "error-handling"
    },
    {
      "file": "plugins/cupertino/hooks/pretooluse_guard.py",
      "line": 31,
      "severity": "Low",
      "title": "Import failure is swallowed without capturing the exception, so the eventual denial cannot name the internal error",
      "evidence": "try:\n    import validators  # type: ignore  # noqa: E402\nexcept Exception:\n    validators = None  # handled defensively below; schema checks degrade to \"deny\" on write, not \"skip\"\n...\nif validators is None:\n    deny(\"cupertino: validator module failed to load; refusing to write unvalidated persisted state. \" + ESCAPE_HATCH)",
      "mechanical": true,
      "suggestedFix": "Capture the exception at import time (e.g. `except Exception as exc: validators = None; _validators_import_error = exc`) and include `type(exc).__name__: exc` in the deny() message at line 135, so the denial names the actual internal error as the rule requires rather than a generic, uninformative statement.",
      "rule": "Every PreToolUse/hook guard script must wrap its top-level logic in a broad try/except that fails closed on any unexpected exception (deny the action, exit non-zero) rather than failing open or crashing silently, and the denial message must both name the internal error and point to a specific, named escape-hatch mechanism (e.g. an env var or config flag) for overriding it.",
      "dimension": "error-handling"
    },
    {
      "file": "plugins/andon/hooks/test_andon_enforce.py",
      "line": 1,
      "severity": "High",
      "title": "Test file not invoked by any CI workflow step",
      "evidence": "File exists (269 lines, stdlib unittest-style, docstring says 'Run: python3 test_andon_enforce.py') but grep across .github/workflows/*.yml for 'test_andon_enforce', 'pytest', and 'unittest' shows no workflow step names or runs it.",
      "mechanical": false,
      "suggestedFix": "Add a step to .github/workflows/plugin-checks.yml that runs `python3 -m unittest test_andon_enforce -v` with working-directory: plugins/andon/hooks (mirroring the existing symbol-indexer test step at plugin-checks.yml:130-134).",
      "rule": "Every test_*.py file committed under plugins/ or tools/ must be invoked by at least one automated CI workflow step in .github/workflows/ (e.g., via `python3 -m unittest <name>` or a pytest run whose working-directory/rootdir actually reaches it) before the PR that adds it merges; a test file that exists on disk but that no workflow step names is not counted as coverage.",
      "dimension": "testing-coverage"
    },
    {
      "file": "plugins/self-assess/hooks/test_guard_target_edit.py",
      "line": 1,
      "severity": "High",
      "title": "Test file not invoked by any CI workflow step",
      "evidence": "185-line test file for the PreToolUse target-edit guard; no .github/workflows/*.yml step references it, pytest, or unittest for this module.",
      "mechanical": false,
      "suggestedFix": "Add a CI step invoking `python3 -m unittest test_guard_target_edit -v` with working-directory: plugins/self-assess/hooks.",
      "rule": "Every test_*.py file committed under plugins/ or tools/ must be invoked by at least one automated CI workflow step in .github/workflows/ (e.g., via `python3 -m unittest <name>` or a pytest run whose working-directory/rootdir actually reaches it) before the PR that adds it merges; a test file that exists on disk but that no workflow step names is not counted as coverage.",
      "dimension": "testing-coverage"
    },
    {
      "file": "plugins/lehre/hooks/test_lehre_guard.py",
      "line": 1,
      "severity": "High",
      "title": "Test file not invoked by any CI workflow step",
      "evidence": "260-line behavioural test file for lehre_guard's deny/allow behavior; not named by any workflow step, and only mentioned as a code comment in test/plugins/cases.tsv:82, not executed there.",
      "mechanical": false,
      "suggestedFix": "Add a CI step invoking `python3 -m unittest test_lehre_guard -v` with working-directory: plugins/lehre/hooks.",
      "rule": "Every test_*.py file committed under plugins/ or tools/ must be invoked by at least one automated CI workflow step in .github/workflows/ (e.g., via `python3 -m unittest <name>` or a pytest run whose working-directory/rootdir actually reaches it) before the PR that adds it merges; a test file that exists on disk but that no workflow step names is not counted as coverage.",
      "dimension": "testing-coverage"
    },
    {
      "file": "plugins/confab/scripts/hooks/test_guard_edit_scope.py",
      "line": 1,
      "severity": "High",
      "title": "Test file not invoked by any CI workflow step",
      "evidence": "132-line test file for confab's PreToolUse edit-scope guard; absent from every .github/workflows/*.yml run: line.",
      "mechanical": false,
      "suggestedFix": "Add a CI step invoking `python3 -m unittest test_guard_edit_scope -v` with working-directory: plugins/confab/scripts/hooks.",
      "rule": "Every test_*.py file committed under plugins/ or tools/ must be invoked by at least one automated CI workflow step in .github/workflows/ (e.g., via `python3 -m unittest <name>` or a pytest run whose working-directory/rootdir actually reaches it) before the PR that adds it merges; a test file that exists on disk but that no workflow step names is not counted as coverage.",
      "dimension": "testing-coverage"
    },
    {
      "file": "plugins/compass/scripts/test_build_branch_comparison_html.py",
      "line": 1,
      "severity": "High",
      "title": "Test file not invoked by any CI workflow step",
      "evidence": "141-line test file whose own docstring says 'Run: python3 scripts/test_build_branch_comparison_html.py', but no workflow step in .github/workflows/ runs it.",
      "mechanical": false,
      "suggestedFix": "Add a CI step invoking `python3 -m unittest test_build_branch_comparison_html -v` with working-directory: plugins/compass/scripts.",
      "rule": "Every test_*.py file committed under plugins/ or tools/ must be invoked by at least one automated CI workflow step in .github/workflows/ (e.g., via `python3 -m unittest <name>` or a pytest run whose working-directory/rootdir actually reaches it) before the PR that adds it merges; a test file that exists on disk but that no workflow step names is not counted as coverage.",
      "dimension": "testing-coverage"
    },
    {
      "file": "plugins/confab/scripts/test_build_burndown_html.py",
      "line": 1,
      "severity": "High",
      "title": "Test file not invoked by any CI workflow step",
      "evidence": "181-line test file for confab-status's burndown chart builder; not named by any .github/workflows/*.yml step.",
      "mechanical": false,
      "suggestedFix": "Add a CI step invoking `python3 -m unittest test_build_burndown_html -v` with working-directory: plugins/confab/scripts.",
      "rule": "Every test_*.py file committed under plugins/ or tools/ must be invoked by at least one automated CI workflow step in .github/workflows/ (e.g., via `python3 -m unittest <name>` or a pytest run whose working-directory/rootdir actually reaches it) before the PR that adds it merges; a test file that exists on disk but that no workflow step names is not counted as coverage.",
      "dimension": "testing-coverage"
    },
    {
      "file": "plugins/self-assess/scripts/test_build_stage_map_html.py",
      "line": 1,
      "severity": "High",
      "title": "Test file not invoked by any CI workflow step",
      "evidence": "153-line test file for self-assess-stage-map's HTML viewer builder; not named by any .github/workflows/*.yml step.",
      "mechanical": false,
      "suggestedFix": "Add a CI step invoking `python3 -m unittest test_build_stage_map_html -v` with working-directory: plugins/self-assess/scripts.",
      "rule": "Every test_*.py file committed under plugins/ or tools/ must be invoked by at least one automated CI workflow step in .github/workflows/ (e.g., via `python3 -m unittest <name>` or a pytest run whose working-directory/rootdir actually reaches it) before the PR that adds it merges; a test file that exists on disk but that no workflow step names is not counted as coverage.",
      "dimension": "testing-coverage"
    },
    {
      "file": "plugins/compass/scripts/test_compass.py",
      "line": 1,
      "severity": "High",
      "title": "Test file not invoked by any CI workflow step",
      "evidence": "292-line self-contained test suite for compass_lib guards ('Run: python3 scripts/test_compass.py'); no workflow step names or runs it.",
      "mechanical": false,
      "suggestedFix": "Add a CI step invoking `python3 -m unittest test_compass -v` with working-directory: plugins/compass/scripts.",
      "rule": "Every test_*.py file committed under plugins/ or tools/ must be invoked by at least one automated CI workflow step in .github/workflows/ (e.g., via `python3 -m unittest <name>` or a pytest run whose working-directory/rootdir actually reaches it) before the PR that adds it merges; a test file that exists on disk but that no workflow step names is not counted as coverage.",
      "dimension": "testing-coverage"
    },
    {
      "file": "plugins/confab/scripts/test_cycle_engine.py",
      "line": 1,
      "severity": "High",
      "title": "Test file not invoked by any CI workflow step",
      "evidence": "134-line test file for confab-cycle's constraint-domain picker; no workflow step in .github/workflows/*.yml runs it.",
      "mechanical": false,
      "suggestedFix": "Add a CI step invoking `python3 -m unittest test_cycle_engine -v` with working-directory: plugins/confab/scripts.",
      "rule": "Every test_*.py file committed under plugins/ or tools/ must be invoked by at least one automated CI workflow step in .github/workflows/ (e.g., via `python3 -m unittest <name>` or a pytest run whose working-directory/rootdir actually reaches it) before the PR that adds it merges; a test file that exists on disk but that no workflow step names is not counted as coverage.",
      "dimension": "testing-coverage"
    },
    {
      "file": "plugins/lehre/scripts/test_lehre_core.py",
      "line": 1,
      "severity": "High",
      "title": "Test file not invoked by any CI workflow step",
      "evidence": "189-line known-answer test file for lehre_core's evaluator ('Run before trusting any verdict this plugin produces: python3 plugins/lehre/scripts/test_lehre_core.py'); not run by any CI workflow.",
      "mechanical": false,
      "suggestedFix": "Add a CI step invoking `python3 -m unittest test_lehre_core -v` with working-directory: plugins/lehre/scripts.",
      "rule": "Every test_*.py file committed under plugins/ or tools/ must be invoked by at least one automated CI workflow step in .github/workflows/ (e.g., via `python3 -m unittest <name>` or a pytest run whose working-directory/rootdir actually reaches it) before the PR that adds it merges; a test file that exists on disk but that no workflow step names is not counted as coverage.",
      "dimension": "testing-coverage"
    },
    {
      "file": "plugins/cupertino/scripts/test_state.py",
      "line": 1,
      "severity": "High",
      "title": "Test file not invoked by any CI workflow step",
      "evidence": "121-line test file for cupertino's state.py flag store; not named or run by any .github/workflows/*.yml step.",
      "mechanical": false,
      "suggestedFix": "Add a CI step invoking `python3 -m unittest test_state -v` with working-directory: plugins/cupertino/scripts.",
      "rule": "Every test_*.py file committed under plugins/ or tools/ must be invoked by at least one automated CI workflow step in .github/workflows/ (e.g., via `python3 -m unittest <name>` or a pytest run whose working-directory/rootdir actually reaches it) before the PR that adds it merges; a test file that exists on disk but that no workflow step names is not counted as coverage.",
      "dimension": "testing-coverage"
    },
    {
      "file": "plugins/self-assess/scripts/lib/test_staleness.py",
      "line": 1,
      "severity": "High",
      "title": "Test file not invoked by any CI workflow step",
      "evidence": "126-line test file for self-assess-autopilot's stage-map freshness check; not named or run by any .github/workflows/*.yml step.",
      "mechanical": false,
      "suggestedFix": "Add a CI step invoking `python3 -m unittest test_staleness -v` with working-directory: plugins/self-assess/scripts/lib.",
      "rule": "Every test_*.py file committed under plugins/ or tools/ must be invoked by at least one automated CI workflow step in .github/workflows/ (e.g., via `python3 -m unittest <name>` or a pytest run whose working-directory/rootdir actually reaches it) before the PR that adds it merges; a test file that exists on disk but that no workflow step names is not counted as coverage.",
      "dimension": "testing-coverage"
    },
    {
      "file": "plugins/self-assess/scripts/lib/test_status.py",
      "line": 1,
      "severity": "High",
      "title": "Test file not invoked by any CI workflow step",
      "evidence": "99-line test file for self-assess-status's dashboard builder; not named or run by any .github/workflows/*.yml step.",
      "mechanical": false,
      "suggestedFix": "Add a CI step invoking `python3 -m unittest test_status -v` with working-directory: plugins/self-assess/scripts/lib.",
      "rule": "Every test_*.py file committed under plugins/ or tools/ must be invoked by at least one automated CI workflow step in .github/workflows/ (e.g., via `python3 -m unittest <name>` or a pytest run whose working-directory/rootdir actually reaches it) before the PR that adds it merges; a test file that exists on disk but that no workflow step names is not counted as coverage.",
      "dimension": "testing-coverage"
    },
    {
      "file": "tools/catalog-validator/test_validate_catalog.py",
      "line": 1,
      "severity": "High",
      "title": "Test file not invoked by any CI workflow step",
      "evidence": "798-line unittest suite for validate_catalog.py; plugin-checks.yml only runs `python3 tools/catalog-validator/validate_catalog.py` (line 144, the validator itself, not its tests) -- the test_ file is never invoked by unittest or pytest in any workflow.",
      "mechanical": false,
      "suggestedFix": "Add a CI step invoking `python3 -m unittest test_validate_catalog -v` with working-directory: tools/catalog-validator (near the existing validate-catalog step in plugin-checks.yml).",
      "rule": "Every test_*.py file committed under plugins/ or tools/ must be invoked by at least one automated CI workflow step in .github/workflows/ (e.g., via `python3 -m unittest <name>` or a pytest run whose working-directory/rootdir actually reaches it) before the PR that adds it merges; a test file that exists on disk but that no workflow step names is not counted as coverage.",
      "dimension": "testing-coverage"
    },
    {
      "file": "tools/andon-ledger-validator/test_validate_ledger.py",
      "line": 1,
      "severity": "High",
      "title": "Test file not invoked by any CI workflow step",
      "evidence": "181-line unittest suite ('Run: python3 tools/andon-ledger-validator/test_validate_ledger.py'); no .github/workflows/*.yml step names or runs it.",
      "mechanical": false,
      "suggestedFix": "Add a CI step invoking `python3 -m unittest test_validate_ledger -v` with working-directory: tools/andon-ledger-validator.",
      "rule": "Every test_*.py file committed under plugins/ or tools/ must be invoked by at least one automated CI workflow step in .github/workflows/ (e.g., via `python3 -m unittest <name>` or a pytest run whose working-directory/rootdir actually reaches it) before the PR that adds it merges; a test file that exists on disk but that no workflow step names is not counted as coverage.",
      "dimension": "testing-coverage"
    },
    {
      "file": "tools/enforcement-audit/test_audit_enforcement.py",
      "line": 1,
      "severity": "High",
      "title": "Test file not invoked by any CI workflow step",
      "evidence": "159-line test suite for the enforcement auditor; no .github/workflows/*.yml step names or runs it, despite CLAUDE.md describing this auditor as hand-checked/calibrated.",
      "mechanical": false,
      "suggestedFix": "Add a CI step invoking `python3 -m unittest test_audit_enforcement -v` with working-directory: tools/enforcement-audit.",
      "rule": "Every test_*.py file committed under plugins/ or tools/ must be invoked by at least one automated CI workflow step in .github/workflows/ (e.g., via `python3 -m unittest <name>` or a pytest run whose working-directory/rootdir actually reaches it) before the PR that adds it merges; a test file that exists on disk but that no workflow step names is not counted as coverage.",
      "dimension": "testing-coverage"
    },
    {
      "file": "tools/surface-index/test_build_surface_index.py",
      "line": 1,
      "severity": "High",
      "title": "Test file not invoked by any CI workflow step",
      "evidence": "452-line unit test suite for build_surface_index.py; docs.yml invokes the builder script itself (`python3 tools/surface-index/build_surface_index.py`, docs.yml:61) but never its test_ file via unittest or pytest.",
      "mechanical": false,
      "suggestedFix": "Add a CI step invoking `python3 -m unittest test_build_surface_index -v` with working-directory: tools/surface-index.",
      "rule": "Every test_*.py file committed under plugins/ or tools/ must be invoked by at least one automated CI workflow step in .github/workflows/ (e.g., via `python3 -m unittest <name>` or a pytest run whose working-directory/rootdir actually reaches it) before the PR that adds it merges; a test file that exists on disk but that no workflow step names is not counted as coverage.",
      "dimension": "testing-coverage"
    },
    {
      "file": "tools/werkstoff-cli/pyproject.toml",
      "line": 33,
      "severity": "High",
      "title": "D101/D102/D103 not added to tools/werkstoff-cli's ruff select list",
      "evidence": "select = [\"E\", \"F\", \"I\", \"UP\", \"B\"]",
      "mechanical": true,
      "suggestedFix": "Change line 33 to select = [\"E\", \"F\", \"I\", \"UP\", \"B\", \"D101\", \"D102\", \"D103\"] (scoped to this package's own pyproject.toml only).",
      "rule": "Every public (non-underscore) function and class in tools/werkstoff-cli/src/werkstoff/ \u2014 the repo's one PyPI-published package and its only genuine external \"public API\" \u2014 must carry a docstring describing what it does, its parameters, and its return value; enforce this by adding \"D101\",\"D102\",\"D103\" to that package's own `[tool.ruff.lint] select` list in tools/werkstoff-cli/pyproject.toml (scoped there only, not repo-wide).",
      "dimension": "public-api-documentation"
    },
    {
      "file": "tools/werkstoff-cli/src/werkstoff/core.py",
      "line": 30,
      "severity": "Medium",
      "title": "Public class Plugin has no docstring",
      "evidence": "@dataclass(frozen=True)\nclass Plugin:\n    name: str\n    description: str\n    source: str\n    category: str | None = None",
      "mechanical": false,
      "suggestedFix": "Add a class docstring describing that Plugin represents one marketplace plugin entry and briefly note its fields (name, description, source, category).",
      "rule": "Every public (non-underscore) function and class in tools/werkstoff-cli/src/werkstoff/ \u2014 the repo's one PyPI-published package and its only genuine external \"public API\" \u2014 must carry a docstring describing what it does, its parameters, and its return value; enforce this by adding \"D101\",\"D102\",\"D103\" to that package's own `[tool.ruff.lint] select` list in tools/werkstoff-cli/pyproject.toml (scoped there only, not repo-wide).",
      "dimension": "public-api-documentation"
    },
    {
      "file": "tools/werkstoff-cli/src/werkstoff/core.py",
      "line": 38,
      "severity": "Medium",
      "title": "Public class Marketplace has no docstring",
      "evidence": "@dataclass(frozen=True)\nclass Marketplace:\n    name: str\n    root: Path\n    plugins: tuple[Plugin, ...]",
      "mechanical": false,
      "suggestedFix": "Add a class docstring describing that Marketplace represents a loaded marketplace manifest and its plugins.",
      "rule": "Every public (non-underscore) function and class in tools/werkstoff-cli/src/werkstoff/ \u2014 the repo's one PyPI-published package and its only genuine external \"public API\" \u2014 must carry a docstring describing what it does, its parameters, and its return value; enforce this by adding \"D101\",\"D102\",\"D103\" to that package's own `[tool.ruff.lint] select` list in tools/werkstoff-cli/pyproject.toml (scoped there only, not repo-wide).",
      "dimension": "public-api-documentation"
    },
    {
      "file": "tools/werkstoff-cli/src/werkstoff/core.py",
      "line": 43,
      "severity": "Medium",
      "title": "Public method Marketplace.plugin has no docstring",
      "evidence": "def plugin(self, name: str) -> Plugin:\n    for candidate in self.plugins:\n        if candidate.name == name:\n            return candidate\n    known = \", \".join(p.name for p in self.plugins)\n    raise WerkstoffError(f\"unknown plugin '{name}' (known: {known})\")",
      "mechanical": false,
      "suggestedFix": "Add a docstring documenting the `name` parameter, the returned Plugin, and that it raises WerkstoffError when the name is unknown.",
      "rule": "Every public (non-underscore) function and class in tools/werkstoff-cli/src/werkstoff/ \u2014 the repo's one PyPI-published package and its only genuine external \"public API\" \u2014 must carry a docstring describing what it does, its parameters, and its return value; enforce this by adding \"D101\",\"D102\",\"D103\" to that package's own `[tool.ruff.lint] select` list in tools/werkstoff-cli/pyproject.toml (scoped there only, not repo-wide).",
      "dimension": "public-api-documentation"
    },
    {
      "file": "tools/werkstoff-cli/src/werkstoff/core.py",
      "line": 64,
      "severity": "Medium",
      "title": "Public function load_marketplace has no docstring",
      "evidence": "def load_marketplace(repo_root: Path) -> Marketplace:\n    manifest_path = repo_root / MARKETPLACE_REL_PATH",
      "mechanical": false,
      "suggestedFix": "Add a docstring describing the repo_root parameter, the returned Marketplace, and the WerkstoffError cases raised for a missing/invalid manifest.",
      "rule": "Every public (non-underscore) function and class in tools/werkstoff-cli/src/werkstoff/ \u2014 the repo's one PyPI-published package and its only genuine external \"public API\" \u2014 must carry a docstring describing what it does, its parameters, and its return value; enforce this by adding \"D101\",\"D102\",\"D103\" to that package's own `[tool.ruff.lint] select` list in tools/werkstoff-cli/pyproject.toml (scoped there only, not repo-wide).",
      "dimension": "public-api-documentation"
    },
    {
      "file": "tools/werkstoff-cli/src/werkstoff/core.py",
      "line": 92,
      "severity": "Medium",
      "title": "Public function unknown_plugin_names has no docstring",
      "evidence": "def unknown_plugin_names(marketplace: Marketplace, names: tuple[str, ...]) -> list[str]:\n    known = {p.name for p in marketplace.plugins}\n    return [n for n in names if n not in known]",
      "mechanical": false,
      "suggestedFix": "Add a docstring describing the marketplace/names parameters and that it returns the subset of names not present in the marketplace.",
      "rule": "Every public (non-underscore) function and class in tools/werkstoff-cli/src/werkstoff/ \u2014 the repo's one PyPI-published package and its only genuine external \"public API\" \u2014 must carry a docstring describing what it does, its parameters, and its return value; enforce this by adding \"D101\",\"D102\",\"D103\" to that package's own `[tool.ruff.lint] select` list in tools/werkstoff-cli/pyproject.toml (scoped there only, not repo-wide).",
      "dimension": "public-api-documentation"
    },
    {
      "file": "tools/werkstoff-cli/src/werkstoff/core.py",
      "line": 97,
      "severity": "Medium",
      "title": "Public function ensure_claude_cli has no docstring",
      "evidence": "def ensure_claude_cli() -> str:\n    path = shutil.which(\"claude\")\n    if path is None:\n        raise WerkstoffError(\"the 'claude' CLI was not found on PATH\")\n    return path",
      "mechanical": false,
      "suggestedFix": "Add a docstring describing that it returns the resolved path to the claude CLI, raising WerkstoffError if not found on PATH.",
      "rule": "Every public (non-underscore) function and class in tools/werkstoff-cli/src/werkstoff/ \u2014 the repo's one PyPI-published package and its only genuine external \"public API\" \u2014 must carry a docstring describing what it does, its parameters, and its return value; enforce this by adding \"D101\",\"D102\",\"D103\" to that package's own `[tool.ruff.lint] select` list in tools/werkstoff-cli/pyproject.toml (scoped there only, not repo-wide).",
      "dimension": "public-api-documentation"
    },
    {
      "file": "tools/werkstoff-cli/src/werkstoff/core.py",
      "line": 112,
      "severity": "Medium",
      "title": "Public function add_marketplace has no docstring",
      "evidence": "def add_marketplace(\n    marketplace: Marketplace, run: Runner = subprocess.run\n) -> subprocess.CompletedProcess[str]:\n    claude = ensure_claude_cli()\n    return _run([claude, \"plugin\", \"marketplace\", \"add\", str(marketplace.root)], run)",
      "mechanical": false,
      "suggestedFix": "Add a docstring describing the marketplace/run parameters and the returned CompletedProcess from `claude plugin marketplace add`.",
      "rule": "Every public (non-underscore) function and class in tools/werkstoff-cli/src/werkstoff/ \u2014 the repo's one PyPI-published package and its only genuine external \"public API\" \u2014 must carry a docstring describing what it does, its parameters, and its return value; enforce this by adding \"D101\",\"D102\",\"D103\" to that package's own `[tool.ruff.lint] select` list in tools/werkstoff-cli/pyproject.toml (scoped there only, not repo-wide).",
      "dimension": "public-api-documentation"
    },
    {
      "file": "tools/werkstoff-cli/src/werkstoff/core.py",
      "line": 119,
      "severity": "Medium",
      "title": "Public function update_marketplace has no docstring",
      "evidence": "def update_marketplace(\n    marketplace: Marketplace, run: Runner = subprocess.run\n) -> subprocess.CompletedProcess[str]:\n    claude = ensure_claude_cli()\n    return _run([claude, \"plugin\", \"marketplace\", \"update\", marketplace.name], run)",
      "mechanical": false,
      "suggestedFix": "Add a docstring describing the marketplace/run parameters and the returned CompletedProcess from `claude plugin marketplace update`.",
      "rule": "Every public (non-underscore) function and class in tools/werkstoff-cli/src/werkstoff/ \u2014 the repo's one PyPI-published package and its only genuine external \"public API\" \u2014 must carry a docstring describing what it does, its parameters, and its return value; enforce this by adding \"D101\",\"D102\",\"D103\" to that package's own `[tool.ruff.lint] select` list in tools/werkstoff-cli/pyproject.toml (scoped there only, not repo-wide).",
      "dimension": "public-api-documentation"
    },
    {
      "file": "tools/werkstoff-cli/src/werkstoff/core.py",
      "line": 126,
      "severity": "Medium",
      "title": "Public function install_plugin has no docstring",
      "evidence": "def install_plugin(\n    marketplace: Marketplace,\n    plugin_name: str,\n    scope: str = DEFAULT_SCOPE,\n    run: Runner = subprocess.run,\n) -> subprocess.CompletedProcess[str]:\n    plugin = marketplace.plugin(plugin_name)  # validates the name first\n    claude = ensure_claude_cli()\n    return _run(\n        [claude, \"plugin\", \"install\", f\"{plugin.name}@{marketplace.name}\", \"--scope\", scope],\n        run,\n    )",
      "mechanical": false,
      "suggestedFix": "Add a docstring documenting marketplace/plugin_name/scope/run parameters and the returned CompletedProcess from `claude plugin install`.",
      "rule": "Every public (non-underscore) function and class in tools/werkstoff-cli/src/werkstoff/ \u2014 the repo's one PyPI-published package and its only genuine external \"public API\" \u2014 must carry a docstring describing what it does, its parameters, and its return value; enforce this by adding \"D101\",\"D102\",\"D103\" to that package's own `[tool.ruff.lint] select` list in tools/werkstoff-cli/pyproject.toml (scoped there only, not repo-wide).",
      "dimension": "public-api-documentation"
    },
    {
      "file": "ruff.toml",
      "line": 47,
      "severity": "High",
      "title": "Repo-root ruff config does not enable C901/PLR0915/PLR0912/PLR0913",
      "evidence": "[lint]\nselect = [\"E4\", \"E7\", \"E9\", \"F\"]\n-- no C901 (mccabe), PLR0915, PLR0912, or PLR0913, and no [lint.mccabe] max-complexity setting either.",
      "mechanical": true,
      "suggestedFix": "Add \"C901\", \"PLR0915\", \"PLR0912\", \"PLR0913\" to [lint].select (and optionally add a [lint.mccabe] max-complexity = 10 table, though 10 is ruff's default for C901).",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "tools/werkstoff-cli/pyproject.toml",
      "line": 33,
      "severity": "High",
      "title": "werkstoff-cli's own ruff select list does not enable C901/PLR0915/PLR0912/PLR0913",
      "evidence": "[tool.ruff.lint]\nselect = [\"E\", \"F\", \"I\", \"UP\", \"B\"]\n-- no C901, PLR0915, PLR0912, or PLR0913 codes present.",
      "mechanical": true,
      "suggestedFix": "Add \"C901\", \"PLR0915\", \"PLR0912\", \"PLR0913\" to [tool.ruff.lint].select.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "plugins/andon/scripts/andon_core.py",
      "line": 1019,
      "severity": "Medium",
      "title": "Multiple functions exceed complexity/size limits (would fail C901/PLR0912/PLR0915/PLR0913 once enabled)",
      "evidence": "L132 _parse_scalar C901 (11>10); L163 parse_frontmatter C901 (11>10); L323 validate_doc C901 (19>10) + PLR0912 (19>12); L585 PLR0913 (7>5 args); L829 PLR0913 (9>5 args); L926 render_board C901 (12>10); L1019 main C901 (24>10) + PLR0912 (24>12) + PLR0915 (143>50 statements)",
      "mechanical": false,
      "suggestedFix": "Decompose main() and validate_doc() into smaller helper functions, reduce argument counts on the flagged functions via parameter objects/dataclasses, and simplify branching in _parse_scalar/parse_frontmatter/render_board.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "plugins/andon/scripts/build_symbol_index.py",
      "line": 370,
      "severity": "Medium",
      "title": "Functions exceed PLR0913 argument-count limit",
      "evidence": "L370 build_index PLR0913 (6>5 args); L542 artifact_manifest PLR0913 (6>5 args); L608 publish_snapshot PLR0913 (6>5 args)",
      "mechanical": false,
      "suggestedFix": "Group related parameters into a dataclass/NamedTuple to bring argument counts under the limit.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "plugins/cli-scaffold/scripts/selftest.py",
      "line": 36,
      "severity": "Medium",
      "title": "Function exceeds PLR0915 statement-count limit",
      "evidence": "L36 function body has 180 statements (>50, PLR0915)",
      "mechanical": false,
      "suggestedFix": "Split the test-running function into smaller helper functions grouped by test phase.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "plugins/cli-scaffold/scripts/verify_scaffold.py",
      "line": 469,
      "severity": "Medium",
      "title": "Function exceeds PLR0915 statement-count limit",
      "evidence": "L469 function has 54 statements (>50, PLR0915)",
      "mechanical": false,
      "suggestedFix": "Extract logical sections of the function into helper functions.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "plugins/compass/scripts/build_symbol_index.py",
      "line": 370,
      "severity": "Medium",
      "title": "Functions exceed PLR0913 argument-count limit",
      "evidence": "L370 build_index PLR0913 (6>5 args); L542 artifact_manifest PLR0913 (6>5 args); L608 publish_snapshot PLR0913 (6>5 args)",
      "mechanical": false,
      "suggestedFix": "Group related parameters into a dataclass/NamedTuple to bring argument counts under the limit.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "plugins/compass/scripts/compass_lib.py",
      "line": 249,
      "severity": "Medium",
      "title": "validate_dag exceeds complexity and branch limits",
      "evidence": "L249 validate_dag C901 (16>10) + PLR0912 (15>12)",
      "mechanical": false,
      "suggestedFix": "Extract the individual validation checks (cycle detection, orphan detection, etc.) into separate helper functions.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "plugins/confab/scripts/agentic_reliability.py",
      "line": 56,
      "severity": "Medium",
      "title": "Function exceeds PLR0915 statement-count limit",
      "evidence": "L56 function has 54 statements (>50, PLR0915)",
      "mechanical": false,
      "suggestedFix": "Split the function into smaller helpers by responsibility.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "plugins/confab/scripts/assertion_audit.py",
      "line": 65,
      "severity": "Medium",
      "title": "Function exceeds PLR0915 statement-count limit",
      "evidence": "L65 function has 54 statements (>50, PLR0915)",
      "mechanical": false,
      "suggestedFix": "Split the function into smaller helpers by responsibility.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "plugins/confab/scripts/build_symbol_index.py",
      "line": 370,
      "severity": "Medium",
      "title": "Functions exceed PLR0913 argument-count limit",
      "evidence": "L370 build_index PLR0913 (6>5 args); L542 artifact_manifest PLR0913 (6>5 args); L608 publish_snapshot PLR0913 (6>5 args)",
      "mechanical": false,
      "suggestedFix": "Group related parameters into a dataclass/NamedTuple to bring argument counts under the limit.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "plugins/confab/scripts/code_change_review.py",
      "line": 74,
      "severity": "Medium",
      "title": "main exceeds complexity and statement limits",
      "evidence": "L74 main C901 (12>10) + PLR0915 (51>50 statements)",
      "mechanical": false,
      "suggestedFix": "Extract argument parsing and each review step into separate helper functions.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "plugins/confab/scripts/cycle_engine.py",
      "line": 173,
      "severity": "Medium",
      "title": "Functions exceed complexity/branch limits",
      "evidence": "L105 _pick_constraint_domain C901 (13>10); L173 cmd_record_pass_result C901 (14>10) + PLR0912 (14>12)",
      "mechanical": false,
      "suggestedFix": "Extract branch-heavy logic in _pick_constraint_domain and cmd_record_pass_result into smaller helper functions.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "plugins/confab/scripts/hooks/guard_edit_scope.py",
      "line": 73,
      "severity": "Medium",
      "title": "run exceeds complexity limit",
      "evidence": "L73 run C901 (13>10)",
      "mechanical": false,
      "suggestedFix": "Extract the guard's decision branches into named helper predicates.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "plugins/confab/scripts/lib/ledger.py",
      "line": 70,
      "severity": "Medium",
      "title": "Function exceeds PLR0913 argument-count limit",
      "evidence": "L70 PLR0913 (6>5 args)",
      "mechanical": false,
      "suggestedFix": "Group related parameters into a dataclass/NamedTuple.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "plugins/cupertino/hooks/pretooluse_guard.py",
      "line": 185,
      "severity": "Medium",
      "title": "handle_skill and main exceed complexity limit",
      "evidence": "L185 handle_skill C901 (11>10); L283 main C901 (11>10)",
      "mechanical": false,
      "suggestedFix": "Extract branch groups in handle_skill/main into smaller helper functions.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "plugins/cupertino/scripts/build_symbol_index.py",
      "line": 370,
      "severity": "Medium",
      "title": "Functions exceed PLR0913 argument-count limit",
      "evidence": "L370 build_index PLR0913 (6>5 args); L542 artifact_manifest PLR0913 (6>5 args); L608 publish_snapshot PLR0913 (6>5 args)",
      "mechanical": false,
      "suggestedFix": "Group related parameters into a dataclass/NamedTuple.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "plugins/lehre/hooks/lehre_guard.py",
      "line": 236,
      "severity": "Medium",
      "title": "Multiple functions exceed complexity/branch/statement limits",
      "evidence": "L96 edit_targets C901 (11>10); L187 resulting_content C901 (12>10); L236 main C901 (25>10) + PLR0912 (24>12) + PLR0915 (68>50 statements)",
      "mechanical": false,
      "suggestedFix": "Decompose main() into helper functions per guard stage; simplify edit_targets/resulting_content branching.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "plugins/lehre/scripts/build_doctrine_html.py",
      "line": 90,
      "severity": "Medium",
      "title": "build exceeds complexity limit",
      "evidence": "L90 build C901 (12>10)",
      "mechanical": false,
      "suggestedFix": "Extract HTML-section builders into separate helper functions.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "plugins/lehre/scripts/lehre_cli.py",
      "line": 162,
      "severity": "Medium",
      "title": "cmd_gauge exceeds complexity and branch limits",
      "evidence": "L162 cmd_gauge C901 (17>10) + PLR0912 (17>12)",
      "mechanical": false,
      "suggestedFix": "Split cmd_gauge's per-option handling into smaller dispatched helpers.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "plugins/lehre/scripts/lehre_core.py",
      "line": 409,
      "severity": "Medium",
      "title": "Multiple functions exceed complexity/branch/argument limits",
      "evidence": "L149 validate_ruleset C901 (14>10) + PLR0912 (13>12) + PLR0915 (63>50); L344 _find_constructs C901 (17>10) + PLR0912 (16>12); L390 PLR0913 (6>5 args); L409 evaluate_file C901 (14>10) + PLR0912 (13>12)",
      "mechanical": false,
      "suggestedFix": "Decompose validate_ruleset, _find_constructs, and evaluate_file into smaller helper functions; group L390's parameters into a dataclass.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "plugins/self-assess/hooks/guard_target_edit.py",
      "line": 96,
      "severity": "Medium",
      "title": "run exceeds complexity and branch limits",
      "evidence": "L96 run C901 (15>10) + PLR0912 (14>12)",
      "mechanical": false,
      "suggestedFix": "Extract the guard's condition checks into named helper predicates.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "plugins/self-assess/scripts/build_symbol_index.py",
      "line": 370,
      "severity": "Medium",
      "title": "Functions exceed PLR0913 argument-count limit",
      "evidence": "L370 build_index PLR0913 (6>5 args); L542 artifact_manifest PLR0913 (6>5 args); L608 publish_snapshot PLR0913 (6>5 args)",
      "mechanical": false,
      "suggestedFix": "Group related parameters into a dataclass/NamedTuple.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "plugins/self-assess/scripts/lib/frontmatter.py",
      "line": 34,
      "severity": "Medium",
      "title": "parse_frontmatter exceeds complexity and statement limits",
      "evidence": "L34 parse_frontmatter C901 (13>10) + PLR0915 (56>50 statements)",
      "mechanical": false,
      "suggestedFix": "Split parsing of scalars/lists/nested blocks into separate helper functions.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "plugins/self-assess/scripts/lib/graph.py",
      "line": 4,
      "severity": "Medium",
      "title": "find_cycles exceeds complexity limit",
      "evidence": "L4 find_cycles C901 (12>10)",
      "mechanical": false,
      "suggestedFix": "Extract the Tarjan SCC inner steps (index assignment, lowlink update, stack pop) into helper functions.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "plugins/self-assess/scripts/self_assess_cli.py",
      "line": 269,
      "severity": "Medium",
      "title": "build_parser exceeds PLR0915 statement limit",
      "evidence": "L269 build_parser PLR0915 (133>50 statements)",
      "mechanical": false,
      "suggestedFix": "Split subparser registration into one helper function per subcommand group.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "plugins/takt/hooks/takt_guard.py",
      "line": 195,
      "severity": "Medium",
      "title": "main exceeds complexity, branch, and statement limits",
      "evidence": "L195 main C901 (20>10) + PLR0912 (20>12) + PLR0915 (58>50 statements)",
      "mechanical": false,
      "suggestedFix": "Decompose main()'s decision tree into named helper functions per gate.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "tools/catalog-validator/validate_catalog.py",
      "line": 217,
      "severity": "Medium",
      "title": "strip_non_rendering exceeds complexity limit",
      "evidence": "L217 strip_non_rendering C901 (13>10)",
      "mechanical": false,
      "suggestedFix": "Extract the per-line-type filtering rules into small helper predicates.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "tools/enforcement-audit/audit_enforcement.py",
      "line": 268,
      "severity": "Medium",
      "title": "main exceeds complexity and branch limits",
      "evidence": "L268 main C901 (13>10) + PLR0912 (14>12)",
      "mechanical": false,
      "suggestedFix": "Extract argument parsing and each classification step into helper functions.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "tools/plugin-serializer/extract_behavior.py",
      "line": 215,
      "severity": "Medium",
      "title": "main exceeds complexity limit",
      "evidence": "L215 main C901 (12>10)",
      "mechanical": false,
      "suggestedFix": "Extract argument parsing and extraction steps into helper functions.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "tools/prompt-index/build_prompt_index.py",
      "line": 45,
      "severity": "Medium",
      "title": "parse_readme exceeds complexity limit",
      "evidence": "L45 parse_readme C901 (12>10)",
      "mechanical": false,
      "suggestedFix": "Split README section detection and prompt-block extraction into separate helper functions.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    },
    {
      "file": "tools/symbol-indexer/build_symbol_index.py",
      "line": 370,
      "severity": "Medium",
      "title": "Functions exceed PLR0913 argument-count limit",
      "evidence": "L370 build_index PLR0913 (6>5 args); L542 artifact_manifest PLR0913 (6>5 args); L608 publish_snapshot PLR0913 (6>5 args)",
      "mechanical": false,
      "suggestedFix": "Group related parameters into a dataclass/NamedTuple.",
      "rule": "Every Python function/method must have a cyclomatic complexity of at most 10 and every function/module must stay within an enforced size limit, checked by enabling ruff's `C901` (mccabe, max-complexity = 10) and `PLR0915`/`PLR0912`/`PLR0913` (too-many-statements/branches/arguments) rule codes in both `ruff.toml` and `tools/werkstoff-cli/pyproject.toml`'s `[lint].select`.",
      "dimension": "complexity-limits"
    }
  ]
}
