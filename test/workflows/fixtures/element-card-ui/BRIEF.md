# Brief: the element card

A single card presenting one chemical element for a periodic-table web app: symbol, name,
atomic number, atomic mass, and a small sample photograph.

It should read as one designed object rather than four stacked rows, work on a phone, and stay
legible in both light and dark. Colour and spacing are currently literal values chosen ad hoc.
