{
  "schemaVersion": "2",
  "runId": "ap-2026-09-22-fxb3",
  "problem": {
    "statement": "Recorded-red fixture for AP-CHECK-NOT-RAN (#74): an acceptance check that never runs at all (command not found). HEAD (3f62503) never executes acceptance checks at compile time, so this fixture compiles clean there; the current compiler's --probe-checks runs and classifies each check, and rejects any that did not RAN.",
    "shape": "change",
    "acceptance": [
      { "id": "a1", "criterion": "c", "check": "true" },
      { "id": "a2", "criterion": "c", "check": "definitely_not_a_command_xyz" }
    ]
  },
  "writeScope": ["src/**"],
  "budget": { "totalDispatches": 7, "wallClockMinutes": 25 },
  "phases": [
    {
      "id": "build",
      "kind": "fanout-redundant",
      "pattern": "best-of-n",
      "fanOut": 3,
      "modelTier": "sonnet",
      "mode": "auto",
      "writes": "worktree",
      "agentType": "arbeitsplan:candidate-builder",
      "angles": ["a", "b", "c"],
      "requires": [],
      "marker": "built"
    },
    {
      "id": "referee",
      "kind": "fanout-blind",
      "pattern": "blind-referee",
      "fanOut": 3,
      "modelTier": "sonnet",
      "mode": "auto",
      "writes": "none",
      "agentType": "arbeitsplan:candidate-referee",
      "requires": ["built"],
      "marker": "refereed"
    },
    {
      "id": "land",
      "kind": "single-writer",
      "pattern": "select-then-synthesize",
      "modelTier": "sonnet",
      "mode": "auto",
      "writes": "shared",
      "agentType": "arbeitsplan:synthesizer",
      "requires": ["refereed"],
      "marker": "landed"
    }
  ],
  "backend": { "kind": "in-session", "why": ["writes-shared-tree"], "acknowledgedGaps": [] }
}
