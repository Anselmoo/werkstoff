{
  "schemaVersion": "2",
  "runId": "ap-2026-09-25-fxsu",
  "supersedes": 5,
  "problem": {
    "statement": "Recorded-red fixture for AP-SUPERSEDES-INVALID (#93): a top-level `supersedes` that is not a non-empty runId string. HEAD (e42621b) never looked at this key at all -- an unrecognised top-level key was simply ignored, so `supersedes: 5` compiled clean there; the current compiler's supersedes-chain rule (scripts/rounds.py record follows it) rejects it by name.",
    "shape": "change",
    "acceptance": [
      { "id": "a1", "criterion": "c", "check": "true" }
    ]
  },
  "writeScope": ["src/**"],
  "budget": { "totalDispatches": 7, "wallClockMinutes": 25 },
  "phases": [
    {
      "id": "build",
      "kind": "fanout-redundant",
      "pattern": "best-of-n",
      "fanOut": 3,
      "modelTier": "sonnet",
      "mode": "auto",
      "writes": "worktree",
      "agentType": "arbeitsplan:candidate-builder",
      "angles": ["a", "b", "c"],
      "requires": [],
      "marker": "built"
    },
    {
      "id": "referee",
      "kind": "fanout-blind",
      "pattern": "blind-referee",
      "fanOut": 3,
      "modelTier": "sonnet",
      "mode": "auto",
      "writes": "none",
      "agentType": "arbeitsplan:candidate-referee",
      "requires": ["built"],
      "marker": "refereed"
    },
    {
      "id": "land",
      "kind": "single-writer",
      "pattern": "select-then-synthesize",
      "modelTier": "sonnet",
      "mode": "auto",
      "writes": "shared",
      "agentType": "arbeitsplan:synthesizer",
      "requires": ["refereed"],
      "marker": "landed"
    }
  ],
  "backend": { "kind": "in-session", "why": ["writes-shared-tree"], "acknowledgedGaps": [] }
}
