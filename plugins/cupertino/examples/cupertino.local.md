---
enabled: true
handbook:
  output_dir: docs/cupertino/handbooks
  domains: []
  skip_verification: false
  fix:
    mode: propose
---

# cupertino configuration

Copy this file to `.claude/cupertino.local.md` in the project you're
running `cupertino-handbook-*` skills against, then edit as needed. Every
field above is shown at its default — delete any line to fall back to that
default, or delete this whole file entirely to use all defaults.

Remember to add `.claude/*.local.md` to that project's `.gitignore` — this
file is per-developer configuration, not something to commit.
