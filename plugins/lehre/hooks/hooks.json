{
  "description": "lehre: denies a write that violates a blocking, hook-tier doctrine rule, or that runs ahead of a unit it depends on. type:command, never type:prompt -- a prompt hook asks a model to decide, which is the model-mediated path this exists to replace. Inert until a repository declares a doctrine at .lehre/ruleset.json.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/lehre_guard.py\"",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
