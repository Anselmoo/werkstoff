{
  "description": "vorbild: denies edits under the design root's references/ (invariant I1 — references are read-only, so rules may be extracted while assets are never copied) and denies a write to system/tokens.json or out/ while a spread choice record is unanswered. Denies nothing else. Inert until a repo has the configured design root (.design/ by default).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/vorbild_guard.py\"",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
