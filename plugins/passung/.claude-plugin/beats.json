{
  "$schema": "arbeitsplan/beats/2",
  "plugin": "passung",
  "produces": [
    {
      "marker": "patterns-canonized",
      "after": "passung-canonize",
      "meaning": "the canonical form of each divergent dimension is settled",
      "evidence": {
        "kind": "none",
        "why": "writes analysis/<area>/CANON.json where <area> is the command's own argument, so the path is per-invocation. Its workflow contains no fs calls; the command renders the artifacts itself."
      }
    }
  ],
  "requires": []
}
