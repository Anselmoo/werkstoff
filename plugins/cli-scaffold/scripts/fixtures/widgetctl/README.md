# widgetctl

Demo scaffold used as committed input for cli-scaffold's architecture viewer.
Deliberately incomplete: it has no `--help` snapshot test and no shell
completion, so the report it produces is a partial-coverage one rather than a
clean sweep.
