{
  "description": "nacharbeit: during a fix pass, denies any Write/Edit/MultiEdit outside the per-dispatch fix lock or to an opus-/human-tier file, and any git state change. type:command, never type:prompt -- a prompt hook asks a model to decide, which is the model-mediated path this exists to replace. Inert until analysis/nacharbeit/fix_scope.json exists.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/nacharbeit_guard.py\"",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
